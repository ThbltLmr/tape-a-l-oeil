<div class="navbar navbar-expand-sm navbar-light navbar-lewagon" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_url "bim.jpg" %>);">
  <div class="container-fluid">
    <%= link_to boxers_path, class: "navbar-brand" do %>
      <%= image_tag "logo.png" %>
    <% end %>

    <div id="site-name">
      <h1 class="text-white">Tape à l'oeil</h1>
    </div>

    <div class="d-flex align-items-center">
    <% if user_signed_in? %>
    <div id="greeting-message" class="me-3">
      <h5 class="text-white text-center m-0">Hello <%= current_user.first_name %></h5>
    </div>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
          <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% if current_user.photo.attached? %>
            <%= cl_image_tag(current_user.photo.key, width: 40, height: 40, crop: :thumb, class: "avatar", alt: "dropdown menu") %>
          <% else %>
            <%= image_tag('MMA_1.png', size: 40, class: "avatar", alt: "dropdown menu") %>
          <% end %>
          </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "Profile", "#", class: "dropdown-item" %>
              <%= link_to "Dashboard", "#", class: "dropdown-item" %>
              <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
  </div>
</div>
